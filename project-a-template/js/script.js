// alert("test");

function setup() {
    let canvas = createCanvas(800, 500); // grab canvas
    canvas.parent("p5-canvas-container") // assign its parent to the div
}

function draw() {
    background(random(255));
}